/**
 * Transmission Type Enum
 * 
 * Represents the type of transmission in a truck.
 * Values match frontend expectations.
 */
export enum TransmissionType {
  MANUAL = 'manual',
  AUTOMATIC = 'automatic',
  AUTOMATED_MANUAL = 'automated_manual'
}