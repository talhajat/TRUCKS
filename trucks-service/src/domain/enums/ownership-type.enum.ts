/**
 * Ownership Type Enum
 * 
 * Represents how a truck is owned by the company.
 * Values match frontend expectations.
 */
export enum OwnershipType {
  OWNED = 'owned',
  LEASED = 'leased',
  RENTED = 'rented'
}