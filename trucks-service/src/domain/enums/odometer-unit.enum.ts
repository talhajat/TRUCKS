/**
 * Odometer Unit Enum
 * 
 * Represents the unit of measurement for odometer readings.
 * Values match frontend expectations (using 'km' not 'kilometers').
 */
export enum OdometerUnit {
  MILES = 'miles',
  KM = 'km'  // Frontend uses abbreviated format
}